require "spec_helper"

describe LightServices do
  it "has a version number" do
    expect(LightServices::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
